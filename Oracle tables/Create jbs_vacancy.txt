CREATE TABLE "JBS_VACANCY" (	
	"ID" NUMBER NOT NULL, 
	"JOB_TITLE" VARCHAR2(100 CHAR) NOT NULL, 
	"COMPANY" VARCHAR2(100 CHAR), 
	"LOCATION" VARCHAR2(50 CHAR) NOT NULL, 
	CONSTRAINT "JBS_VACANCY_PK" PRIMARY KEY ("ID"), 
	CONSTRAINT "JBS_VACANCY_UK1" UNIQUE ("JOB_TITLE", "COMPANY", "LOCATION")
);
