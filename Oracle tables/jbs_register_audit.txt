CREATE TABLE "JBS_REGISTER_AUDIT" (	
	"ID" NUMBER NOT NULL, 
	"TIME" VARCHAR2(25 CHAR) NOT NULL, 
	"DBA_USER" VARCHAR2(20 CHAR) NOT NULL, 
	CONSTRAINT "JBS_REGISTER_AUDIT_PK" PRIMARY KEY ("ID"), 
	CONSTRAINT "JBS_REGISTER_AUDIT_FK1" FOREIGN KEY ("ID")
	REFERENCES "JBS_USER" ("ID") ON DELETE CASCADE
);
